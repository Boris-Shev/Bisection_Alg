#include "header.hpp"

using namespace std;

int BisecAlg (int n, double* A, double* x, double eps, double* R) {
n=n; A=A; x=x; eps=eps; R=R;

  return 0;
}


template <typename T>
int Sgn(T val) {
  return (T(0) < val) - (val < T(0));
}
